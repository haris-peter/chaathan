# Server Configuration

# Port to run the server on
PORT=3000

# Host/Domain name for the server
# For local: localhost
# For production: your domain or IP address
HOST=localhost

# Server auto-shutdown duration in milliseconds
# Default: 10800000 (3 hours)
# Maximum: 86400000 (24 hours)
# Set to 0 to disable auto-shutdown (server runs indefinitely)
# Invalid values or values exceeding maximum will default to 3 hours
SERVER_DURATION=10800000
